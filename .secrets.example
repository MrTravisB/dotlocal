#!/usr/bin/env bash

# .secrets.example
#
# This file contains environment variable templates for secrets and API keys.
# These values are sourced by .zshrc but are NOT tracked in git.
#
# SETUP:
# 1. Copy this file: cp .secrets.example .secrets
# 2. Fill in your actual values in .secrets
# 3. The install.sh script will validate that all required variables are set
#
# SECURITY:
# - .secrets is gitignored and never committed
# - Never commit actual secrets to this repo
# - Use 1Password, Secretive, or similar tools for key management

# ============================================================
# REQUIRED SECRETS
# ============================================================
# These must be set for core functionality to work

export GITHUB_TOKEN=""

# ============================================================
# OPTIONAL SECRETS
# ============================================================
# These are optional depending on your workflow and tools

# AI APIs
export OPENAI_API_KEY=""
export ANTHROPIC_API_KEY=""

# Context7 (MCP documentation tool)
export CONTEXT7_API_KEY=""

# Langfuse (LLM observability)
export LANGFUSE_SECRET_KEY=""
export LANGFUSE_PUBLIC_KEY=""
export LANGFUSE_BASEURL=""
